# Local development secrets for enterprise_ai_deployment/examples/agent_dev.yaml.
# Copy to a private .env file outside version control and set real values there.

MY_AGENT_API_KEY=replace-with-local-development-secret
